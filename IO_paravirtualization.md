# QEMU 和 Firecracker 怎么做 I/O 半虚拟化——从零讲起

我会按"为什么需要 → 怎么做 → QEMU 和 Firecracker 各自怎么实现"的顺序讲，全程用类比，不堆术语。

---

## 一、先搞清楚:为什么需要"半虚拟化"

### 1. 虚拟机里的程序怎么访问硬件?

你的虚拟机里跑着一个 guest 操作系统(比如 Linux),它以为自己跑在真实电脑上。当 guest 里的程序要读硬盘、发网络包时,它会像在真机上一样:

> "我往这个硬盘控制器的寄存器写一条命令,硬盘就该把数据读出来"

但**这个"硬盘"其实是假的**——它是 VMM(QEMU 或 Firecracker)模拟出来的。真实数据存在 host 上的一个文件里。

### 2. 第一种做法:完全模拟(最朴素)

VMM 老老实实假装自己是一块真硬盘:
- guest 写"硬盘控制器寄存器" → CPU 触发 VMExit(陷入 host)→ VMM 截获 → VMM 解读这条命令 → VMM 读 host 上的文件 → VMM 把数据放到 guest 内存里 → VMM 模拟"硬盘完成中断" → 恢复 guest 运行。

**问题**:太慢。
- guest 不知道自己在虚拟机里,它按真硬盘的协议走,每读一个扇区就要发好几条寄存器命令;
- 每条命令都引起一次 VMExit + VMM 处理 + VMEnter,**上下文切换巨贵**;
- 真硬盘的协议(IDE、SCSI)是为物理硬件设计的,有很多对虚拟化场景毫无意义的握手。

打个比方:你(VMM)在帮一个不懂中文的朋友(guest)和饭店点菜。朋友坚持用他家乡的方言一字一句念菜单,每念一个字你都要翻译一次。慢死了。

### 3. 第二种做法:半虚拟化(聪明的做法)

既然 guest OS 是开源的(Linux),我们干脆**改 guest 内的驱动**,让它知道"我在虚拟机里,可以用一种特别高效的方式和 VMM 说话"。

这种"专门为虚拟化设计的、guest 和 VMM 心知肚明的高效协议"就叫**半虚拟化**(para-virtualization)。"半"是说:guest 知道自己被虚拟化了(不像"完全虚拟化"里 guest 一无所知)。

继续上面的比方:你和朋友提前约定好一套简化暗号——"想吃米饭就敲两下桌子"。一下子省掉了一堆翻译。

### 4. Linux 世界里这套"暗号"的统一标准:virtio

Linux 社区不希望每家虚拟化厂商都搞一套自己的暗号,所以制定了一个**统一标准叫 virtio**。**QEMU 和 Firecracker 都遵循这个标准**——它们做半虚拟化的方式没有本质不同,区别在工程实现上。

所以理解 I/O 半虚拟化的关键,是先理解 **virtio 这套约定**长什么样。

---

## 二、virtio 的核心机制:一个共享的"传送带"

### 1. 关键想法:共享内存 + 环形队列

guest 和 host 想要交换数据,传统方式是"写寄存器 → 触发陷入 → 来回沟通"。virtio 换了个思路:

> **在 guest 和 host 之间开一块共享内存,做成一条"传送带"(环形队列),双方往传送带上放东西就行了。**

这块共享内存叫 **virtqueue**(virtual queue,虚拟队列)。每种虚拟设备至少有一条 virtqueue:
- 虚拟网卡:一条收包队列 + 一条发包队列;
- 虚拟硬盘:一条命令队列。

### 2. 一次 I/O 怎么走?以"guest 读硬盘 4KB 数据"为例

**guest 这边(virtio 驱动)做的事**:

1. guest 内核分配一块 4KB 内存,准备接收硬盘数据;
2. guest 在 virtqueue 上"放一张订单",订单里写明:
   - 我要读硬盘扇区 N;
   - 读到的数据请放到我刚分配的那块内存里(给出物理地址);
3. guest 写一下一个特殊的寄存器(叫 "kick"),相当于按一下门铃告诉 host:"传送带上有新订单了"。

**host 这边(VMM 里的 virtio backend)做的事**:

4. 门铃响 → VMM 看 virtqueue → 发现一张订单;
5. VMM 翻译订单:"guest 要读扇区 N,放到 guest 物理地址 0x12345000";
6. VMM 知道 guest 的"硬盘"其实是 host 上的某个文件,于是去这个文件里读出对应字节;
7. VMM **直接把数据写进 guest 那块内存**(因为是共享的,VMM 可以直接访问 guest 的物理内存);
8. VMM 在 virtqueue 的"完成区"里写一条:"订单 X 已完成";
9. VMM 通知 guest:"你的订单好了"(注入一个虚拟中断);
10. guest 收到中断 → 看完成区 → 知道数据已经在那块内存里了 → 接着干活。

### 3. 为什么这样快?

对比一下完全模拟:

| 阶段 | 完全模拟 | virtio |
|---|---|---|
| guest 发起请求 | 写很多次寄存器,每次都 VMExit | 在共享内存写一张订单,**只 kick 一次** |
| 数据搬运 | guest→VMM→guest 两次拷贝 | VMM 直接写进 guest 内存,**零拷贝** |
| 一次能处理几个请求 | 一次一个 | **一次可以提交一批**(订单写一摞再 kick) |
| guest 等待方式 | 同步等中断 | 提交后可以继续干别的,**异步** |

打个比方:完全模拟像每点一道菜就喊一次服务员;virtio 像服务员给你一个点菜板,你写满一整页再按一下呼叫铃。

### 4. 还差一件事:guest 怎么"发现"这些虚拟设备?

guest 内核启动时要发现"哦,我有一块虚拟硬盘"才能去用它。这个"被发现"的方式叫 **transport**。

最常用的两种 transport:
- **virtio-PCI**:虚拟设备装扮成 PCI 设备,guest 像扫描真 PCI 总线那样发现它们;
- **virtio-MMIO**:虚拟设备直接占用一段固定的内存地址,guest 内核启动参数里告诉它"地址 0xd0000000 这里有一个 virtio 块设备"。

PCI 更通用但代码复杂;MMIO 极简但需要预先告诉 guest 设备的位置。**这就是 QEMU 和 Firecracker 第一个差异点**。

---

## 三、QEMU 怎么做 I/O 半虚拟化

QEMU 是个**老牌、大而全**的 VMM。它支持 virtio 的方式有几个特点:

### 1. transport 全都支持

PCI、MMIO、CCW(IBM 大型机)都行。在 x86 普通虚拟机里默认用 **virtio-PCI**——因为它和真实 PC 体验一致,guest 内核扫 PCI 总线就能自动发现。

### 2. 设备种类多

QEMU 实现了 virtio 规范里几乎所有标准设备:
- virtio-net(网卡)
- virtio-blk(块设备)
- virtio-scsi(SCSI 控制器)
- virtio-vsock(host-guest 直连 socket)
- virtio-gpu(虚拟显卡)
- virtio-fs(共享文件系统)
- virtio-balloon、virtio-rng、virtio-console 等等

### 3. backend 可以选不同位置,有快有慢

这是 QEMU 真正的特色。**"谁来处理 virtqueue 上的订单"这件事,QEMU 提供了好几个选项**:

**选项 A:在 QEMU 进程里处理(最朴素)**
- guest kick → 通知 QEMU 主循环 → QEMU 处理 → 完成;
- 简单,但每次都要从内核切换到 QEMU 用户态进程,慢。

**选项 B:vhost——把处理搬进 Linux 内核**
- QEMU 启动时告诉 Linux 内核里的 `vhost` 模块:"这块 virtqueue 的内存地址在这里,你直接处理吧";
- 之后 guest kick → 直接唤醒 **内核里的 vhost 线程** 处理 → 不再经过 QEMU 用户态;
- 快得多,因为省掉了内核↔用户态切换;
- 这就是为什么生产环境的 QEMU 网卡几乎都用 `vhost-net`。

**选项 C:vhost-user——把处理搬到另一个用户态程序**
- 让一个独立的程序(比如 DPDK、SPDK)来处理 virtqueue;
- QEMU 只负责一开始把"传送带"的位置告诉这个程序;
- 之后 QEMU 完全不参与 datapath;
- 用来做 100Gbps 网络这种极致性能场景。

**选项 D:vDPA——交给硬件**
- 现在有些智能网卡直接懂 virtio 协议;
- guest 的订单被硬件 DMA 直接处理;
- QEMU 只负责初始化,连软件 datapath 都没有。

**理解 QEMU 的关键比喻**:QEMU 像一家配齐了所有厨房设备的大饭店——同样一道菜(virtio 标准),可以让大厨手工炒(选项 A)、用半自动设备(B)、外包给隔壁餐馆(C)、或者用全自动机器人(D)。客人不知道差别,但出菜速度差很多。

---

## 四、Firecracker 怎么做 I/O 半虚拟化

Firecracker 是 **AWS 为 serverless 场景写的极简 VMM**,设计哲学完全相反:**只支持最少的功能,但都做到最稳最快启动**。

### 1. transport:只用 virtio-MMIO,不用 PCI

为什么?因为 PCI 总线的模拟代码量大、历史漏洞多。Firecracker 直接说:"我不模拟 PCI 总线,所有虚拟设备都用 MMIO,地址在启动时写死告诉 guest"。

具体怎么告诉 guest?Firecracker 启动 guest 时,通过 Linux 内核命令行参数加几个字段,比如:
```
virtio_mmio.device=4K@0xd0000000:5
```
意思是"地址 0xd0000000 这里有一个 virtio 设备,中断号 5"。guest 内核的 `virtio_mmio` 驱动启动时按这个去初始化。

### 2. 设备只支持 4–5 种

- virtio-net(网卡)
- virtio-blk(块设备)
- virtio-vsock(host-guest socket 通信)
- virtio-balloon(较新版本才加,用来回收内存)
- 一个串口(用来打印 console,不是 virtio)

**没有** USB、PCI 设备、显卡、声卡、virtio-scsi、virtio-fs、virtio-gpu。

为什么砍这么狠?因为 serverless 函数容器只需要"能跑 Linux、有网、有盘",其他都用不上。砍掉的设备 = 砍掉的代码 = 砍掉的漏洞。

### 3. backend:只有一种——在 Firecracker 进程里处理

这是 Firecracker 和 QEMU 最大的不同。**Firecracker 只支持 QEMU 的"选项 A"**——所有 virtqueue 的处理都在 Firecracker 这个用户态进程内部,由 Rust 代码完成。

具体流程:
1. guest kick → KVM 内核模块通过一个 eventfd(一种 Linux 进程间事件通知机制)通知 Firecracker;
2. Firecracker 主循环用 epoll 监听这些 eventfd,醒来 → 读 virtqueue → 处理订单;
3. 网卡订单:把数据写到 host 上的 tap 设备(一个虚拟网卡);块设备订单:读写 host 上的文件;
4. 处理完通过 irqfd 注入虚拟中断给 guest。

**不使用 vhost、不使用 vhost-user、不使用 vDPA**。

### 4. 为什么 Firecracker 不用更快的 vhost?

虽然 vhost 几乎"白送"性能,Firecracker 团队故意不用,原因主要是:

- **代码简单更重要**:vhost 是 Linux 内核模块,引入它意味着 VM 隔离边界要把 host 内核里的 vhost 代码也算进来;Firecracker 想保持"我自己用 Rust 写的几千行就是全部";
- **性能够用就行**:serverless 场景下单个 VM 跑几十秒到几分钟,网络也就几 Gbps,用不上 100Gbps;
- **隔离一致**:每个 VM 一个 Firecracker 进程,可以用 Linux 沙箱机制(seccomp、jailer)把它关得很死。如果用 vhost,部分逻辑跑在内核里,沙箱套不上。

**理解 Firecracker 的关键比喻**:Firecracker 像一家**只卖三明治的快餐店**——菜单只有 4 样、厨房只有一个微波炉(QEMU 选项 A)、装修极简。出餐快(启动 <125ms)、开店成本低(<5MB 内存)、不容易出食品安全问题(攻击面小),代价是你想吃别的东西它没有。

---

## 五、把 QEMU 和 Firecracker 放在一起对比

下面这张表把两者在 I/O 半虚拟化上的差异列清:

| 维度 | QEMU | Firecracker |
|---|---|---|
| **是否用 virtio 标准** | 是 | 是 |
| **支持的 transport** | virtio-PCI / MMIO / CCW 都行 | **只有 virtio-MMIO** |
| **virtio 设备种类** | 几乎全套(十几种) | **只有 4–5 种** |
| **backend 在哪里处理** | 4 种选择:QEMU 进程 / 内核 vhost / 外部用户态 / 硬件 | **只在 Firecracker 进程内** |
| **最高性能** | 100 Gbps(vDPA) | 10–30 Gbps |
| **代码语言** | C | Rust |
| **virtio 相关代码量** | 几十万行 | 几千行 |
| **启动时间** | 1–5 秒 | <125 ms |
| **典型用户** | 通用云、桌面虚拟机、OpenStack | AWS Lambda、Fargate、Kata Containers |

### 一句话总结二者关系

**QEMU 和 Firecracker 都用 virtio 这个统一标准做 I/O 半虚拟化——guest 看到的虚拟硬盘、虚拟网卡是完全兼容的同一套规范。差别只在:**

- **QEMU 是"功能全开 + 多档加速可选"**:什么设备都支持、什么 transport 都支持、backend 处理位置从慢到快有四档可调;
- **Firecracker 是"功能极简 + 单一处理路径"**:只支持最少的设备、只用 MMIO、所有处理都在自己进程里做;

**两者的差异不是"协议不同",而是"工程上选了不同的取舍"**——QEMU 选灵活与性能上限,Firecracker 选简单、快启动、小内存。

---

## 六、最后一个常见疑惑:guest OS 里需要装什么吗?

需要——guest 的 Linux 内核必须包含 **virtio 驱动**(`virtio_net`、`virtio_blk`、`virtio_mmio`、`virtio_pci` 等模块)。

好消息是 **现代 Linux 内核早就自带了**——只要你装的是 2010 年后的发行版,这些驱动都是默认开启的,所以你启动 QEMU 或 Firecracker 时,guest 里完全不需要额外配置就能看到"我有一块 virtio 硬盘"。

如果是 Windows guest,需要安装 **virtio-win** 驱动包(社区维护),否则只能用慢的完全模拟模式。

到这里 I/O 半虚拟化这个知识点就讲完了。**记住三层结构**:
1. **底层标准**:virtio——共享内存 + 环形队列 + 双方约定的高效协议;
2. **QEMU 的玩法**:全套设备 + 4 种 backend,可慢可快、可灵活组合;
3. **Firecracker 的玩法**:极少设备 + 唯一 backend(自己进程内),追求简单和快启动。

---

## 七、最常用配置下的执行流程图:QEMU + virtio-net + vhost-net 发包

下面用**生产云环境最主流的配置**——QEMU 配 virtio-net 网卡、backend 用 vhost-net(内核加速)——画一遍 guest 发送一个网络包的完整流程。这是目前几乎所有 OpenStack / 自建 KVM 云的默认网络路径。

### 7.1 启动前的准备(只做一次)

```
                       Host 物理机
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              Host Linux 内核                            │  │
│  │  ┌──────────┐  ┌──────────┐  ┌────────────────────┐    │  │
│  │  │   KVM    │  │ vhost-net│  │ tap0 (虚拟网卡)    │────┼──┼─→ 物理网卡
│  │  │  模块    │  │  模块    │  │                    │    │  │
│  │  └──────────┘  └──────────┘  └────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  QEMU 进程 (host 用户态)                                │  │
│  │  · 启动时 mmap 一块内存当作 guest 物理内存              │  │
│  │  · 在这块内存里和 guest 约定好 virtqueue 的位置         │  │
│  │  · 通过 ioctl 把 virtqueue 地址告诉 vhost-net          │  │
│  │  · 创建两个 eventfd:                                    │  │
│  │      - kickfd: guest 通知 host "有新包要发"            │  │
│  │      - callfd: host 通知 guest "有包收到了"            │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  Guest VM                                               │  │
│  │  ┌────────────────────────────────────────────────┐    │  │
│  │  │ Guest Linux 内核                               │    │  │
│  │  │  · virtio_pci 驱动发现虚拟网卡                  │    │  │
│  │  │  · virtio_net 驱动初始化 virtqueue             │    │  │
│  │  └────────────────────────────────────────────────┘    │  │
│  │  ┌────────────────────────────────────────────────┐    │  │
│  │  │ Guest 用户程序 (curl / nginx / ...)             │    │  │
│  │  └────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
```

**准备完毕后,virtqueue 是 guest 内核和 host 内核 vhost-net 都能直接读写的共享内存。**

### 7.2 一次发包的 9 步流程

假设 guest 里的 `curl` 要发一个 HTTP 请求,数据包 1500 字节:

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   Guest 用户态: curl                                                    │
│         │                                                               │
│         │  ① write(socket, data, 1500)                                  │
│         ▼                                                               │
│   ┌─────────────────────────────────────────┐                           │
│   │ Guest 内核: TCP/IP 协议栈               │                           │
│   │ · 组装 TCP/IP 头                        │                           │
│   │ · 调用 virtio_net 驱动发送              │                           │
│   └─────────────────────────────────────────┘                           │
│         │                                                               │
│         │  ② virtio_net 驱动把数据包地址写进 virtqueue 的描述符表       │
│         │     更新 Available Ring: "第 N 号描述符可处理了"              │
│         ▼                                                               │
│   ┌═════════════════════════════════════════════════════════════════┐   │
│   ║         virtqueue (共享内存,guest 和 host 都能读写)            ║   │
│   ║  ┌─────────────────┬─────────────────┬─────────────────────┐    ║   │
│   ║  │ Descriptor 表    │ Available Ring  │ Used Ring           │    ║   │
│   ║  │ [addr,len,flags] │ [idx=N, ...]    │ [完成的 idx, ...]    │    ║   │
│   ║  └─────────────────┴─────────────────┴─────────────────────┘    ║   │
│   ╚═════════════════════════════════════════════════════════════════╝   │
│         │                                                               │
│         │  ③ guest 写 MMIO/PCI 寄存器 "kick"                            │
│         ▼                                                               │
│   ┌─────────────────────────────────────────┐                           │
│   │ KVM 内核模块                             │                           │
│   │ · 截获这次寄存器写                       │                           │
│   │ · 通过 ioeventfd 机制 signal kickfd     │                           │
│   │   (不退出到 QEMU 用户态!)                │                           │
│   └─────────────────────────────────────────┘                           │
│         │                                                               │
│         │  ④ kickfd 被 signal                                           │
│         ▼                                                               │
│   ┌─────────────────────────────────────────┐                           │
│   │ vhost-net 内核线程 (epoll 醒来)         │                           │
│   │ · 读 Available Ring 找到新描述符        │                           │
│   │ · 顺描述符拿到 guest 内存里的数据包      │                           │
│   │ · 零拷贝:不复制,直接交给下一步         │                           │
│   └─────────────────────────────────────────┘                           │
│         │                                                               │
│         │  ⑤ 把数据包写进 tap0 虚拟网卡                                 │
│         ▼                                                               │
│   ┌─────────────────────────────────────────┐                           │
│   │ tap0 → Linux 网桥/OVS → 物理网卡 → 网络 │                           │
│   └─────────────────────────────────────────┘                           │
│         │                                                               │
│         │  ⑥ 发送完成后,vhost-net 在 Used Ring 写一条                  │
│         │     "描述符 N 已处理完"                                       │
│         ▼                                                               │
│   ┌═════════════════════════════════════════════════════════════════┐   │
│   ║         virtqueue (Used Ring 被更新)                            ║   │
│   ╚═════════════════════════════════════════════════════════════════╝   │
│         │                                                               │
│         │  ⑦ vhost-net signal callfd                                    │
│         ▼                                                               │
│   ┌─────────────────────────────────────────┐                           │
│   │ KVM 内核模块                             │                           │
│   │ · irqfd 机制把 callfd 翻译成虚拟中断    │                           │
│   │ · 直接注入 guest 的虚拟 CPU             │                           │
│   └─────────────────────────────────────────┘                           │
│         │                                                               │
│         │  ⑧ guest 虚拟 CPU 收到中断                                    │
│         ▼                                                               │
│   ┌─────────────────────────────────────────┐                           │
│   │ Guest 内核: virtio_net 中断处理函数      │                           │
│   │ · 读 Used Ring → 知道哪些包发完了        │                           │
│   │ · 回收对应内存                           │                           │
│   │ · 唤醒等待的 socket                      │                           │
│   └─────────────────────────────────────────┘                           │
│         │                                                               │
│         │  ⑨ curl 的 write() 返回                                       │
│         ▼                                                               │
│   curl 继续执行                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 7.3 这个流程里最关键的三个性能优化点

**优化 1:datapath 完全绕开 QEMU 用户态(步骤 ③→④→⑤)**

注意整条数据路径上,**QEMU 进程一次都没被唤醒**。guest kick 经 KVM 直接到 vhost-net 内核线程,发包后中断也是 KVM 直接注入。QEMU 只在最开始做了"把 virtqueue 地址告诉 vhost"这一次性配置。

这就是 vhost-net 比"纯 QEMU userspace backend"快几倍的原因——少了两次"内核 ↔ 用户态"切换。

**优化 2:零拷贝(步骤 ④→⑤)**

vhost-net 拿到描述符后,直接用描述符里的 guest 物理地址访问数据包内存——**不做任何复制**。数据从 guest 内核直接走到 tap → 物理网卡。

**优化 3:批量化和异步(步骤 ②、⑥)**

guest 可以一次把多个包的描述符写进 Available Ring 再 kick 一次;vhost 也可以一次处理一批再统一更新 Used Ring 和注入中断。**减少 kick/中断次数 = 减少 VMExit/VMEnter 次数**。

### 7.4 如果换成 Firecracker 配置,这个流程哪里不同?

只有两个地方变:

| 步骤 | QEMU + vhost-net | Firecracker |
|---|---|---|
| ③ guest kick 的方式 | 写 virtio-PCI 寄存器 | 写 virtio-MMIO 寄存器 |
| ④–⑤ 谁处理 virtqueue | **内核 vhost-net 线程** | **Firecracker 用户态进程的 epoll 线程** |

其余完全相同——共享内存 virtqueue、描述符表、Available/Used Ring、零拷贝、中断注入机制都一样。

也就是说,**Firecracker 的流程相当于 QEMU 选了"选项 A:在 VMM 进程里处理",外加把 PCI 换成 MMIO**。所以单包延迟会比 vhost-net 路径长一点(多两次内核↔用户态切换),但代码量小一个数量级、攻击面小很多。

### 7.5 用一句话总结这张图

**guest 把"要发的包"放进共享传送带 → 按一下门铃 → host 端的处理者拿出来真发出去 → 在共享传送带上写"已完成" → 拍 guest 一下肩膀通知它**——这就是 virtio 半虚拟化的全部本质。QEMU 和 Firecracker 的差别只在于"host 端的处理者是谁、住在哪一层"。
